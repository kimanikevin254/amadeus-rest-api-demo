# Making Cross-Platform REST API Calls with Kotlin

This repository demonstrates how to build a REST API with Kotlin to handle travel data using the Amadeus API. Check out the `main` branch for the full project.